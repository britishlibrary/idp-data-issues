// On Rest Authentication Method
// Julian Cook, 28.4.2023
// Only allow Administator to authenticate 
C_TEXT($1; $2; $3)
C_BOOLEAN($0)

//$1: user
//$2: password
//{$3: IP address of client}


//ON ERR CALL("SQL_error")  // must make sure this Project method exists 

If ($1="Administrator") & ($2="Mansell")
	CHANGE CURRENT USER("Administrator"; "Mansell")
	$0:=True
Else 
	$0:=False
End if 